#include "arduino_secrets.h"
/*
  Sketch generated by the Arduino IoT Cloud Thing "Untitled"
  https://create.arduino.cc/cloud/things/081b73eb-bf9b-4fba-afdf-3100e925200d

  Arduino IoT Cloud Variables description

  The following variables are automatically generated and updated when changes are made to the Thing

  float batteryVoltage;
  int weaponSpeed;
  int wheelSpeed;

  Variables which are marked as READ/WRITE in the Cloud Thing will also have functions
  which are called when their values are changed from the Dashboard.
  These functions are generated with the Thing and added at the end of this sketch.
*/
#include <iostream>
#include <string>
using namespace std;
#include "thingProperties.h"
#define RXD2 16
#define TXD2 17
String receivedString = "";
String voltage = "";
String weapon = "";
String leftcurrent = "";
String rightcurrent = "";
String disconnected = "";

void setup()
{
  // Initialize serial and wait for port to open:
  Serial.begin(115200);
  Serial2.begin(9600, SERIAL_8N1, RXD2, TXD2);
  // This delay gives the chance to wait for a Serial Monitor without blocking if none is found
  delay(100);
  // Defined in thingProperties.h
  initProperties();
  // Connect to Arduino IoT Cloud
  ArduinoCloud.begin(ArduinoIoTPreferredConnection);
  /*
     The following function allows you to obtain more information
     related to the state of network and IoT Cloud connection and errors
     the higher number the more granular information you’ll get.
     The default is 0 (only errors).
     Maximum is 4
 */
  setDebugMessageLevel(2);
  ArduinoCloud.printDebugInfo();
}
void loop()
{
  ArduinoCloud.update();
  
  if (Serial2.available()) {
    
    char incomingChar = Serial2.read();
    Serial.print(incomingChar);
    if (incomingChar == 'v'){
      while (incomingChar != ';'){
        voltage += Serial2.read();
      }
      receivedString += voltage ;
    }
    else if (incomingChar == 'l'){
      while (incomingChar != ';'){
        leftcurrent += Serial2.read();
      }
      receivedString += leftcurrent;
    }
    else if(incomingChar == 'r'){
      while (incomingChar != ';'){
        rightcurrent += Serial2.read();
      }
      receivedString += rightcurrent;
    }
    else if(incomingChar == 'w'){
      while (incomingChar != ';'){
        weapon += Serial2.read();
      }
      receivedString += weapon;
    }
    else if(incomingChar == 'd'){
      while (incomingChar != ';'){
        disconnected += Serial2.read();
      }
      receivedString += disconnected;
    }
    else if (incomingChar == '\n') {
      // End of the string, print it and clear the buffer
      Serial.println(receivedString);
      receivedString = "";
      voltage = "";
      disconnected = "";
      rightcurrent = "";
      leftcurrent = "";
      weapon = "";
    } 
  }
  batteryVoltage = voltage.toFloat();
  weaponSpeed = weapon.toInt();
  leftCurrent = leftcurrent.toFloat();
  rightCurrent = rightcurrent.toFloat();
  disconnectedBool = disconnected.toInt();
  if (disconnectedBool == 0){
    disconnectedBool = false;
  }
  else{
    disconnectedBool = true;
  }
  Serial.println("Transmitting data: ");
  Serial.println(batteryVoltage);
  Serial.println(weaponSpeed);
  Serial.println(leftCurrent);
  Serial.println(rightCurrent);
  Serial.println(disconnectedBool);

  Serial.println(voltage);
  Serial.println(weapon);
  Serial.println(leftcurrent);
  Serial.println(rightcurrent);
  Serial.println(disconnected);

}
